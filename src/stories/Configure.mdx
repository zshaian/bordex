import { Meta } from '@storybook/blocks';

<Meta title="Introduction" />
<div>
  <div class="logo-container">
    <img src="./src/stories/assets/bordex-logo.png" height={60} />
    <h1 class="logo-container__title">bordex</h1>
  </div>
  A simple React component library for addding styled borders to your elements.
</div>

## Installation

```bash
npm install bordex
```

## Example

Adding a Gradient Border to an element

```tsx
import { GradientBorder } from 'bordex';

export default function App() {
  return (
    <GradientBorder
      angle="45deg"
      colors={['red', 'green', 'blue']}
      borderWidth="5px"
    >
      <p>Gradient Border</p>
    </GradientBorder>
  );
}
```

![Gradient Border Example](./src/assets/example_border.jpg)

<style>
  {`
    .logo-container {
      display:flex;
      align-items:center;
      gap:16px;
    }
    .logo-container__title {
      text-transform:capitalize;
    }
`}
</style>
